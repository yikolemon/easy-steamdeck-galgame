# PyInstaller 打包指南 - SteamDeck

## 概述

使用 PyInstaller 将 Python 应用打包成**单独的可执行文件**，用户在 SteamDeck 上可以**直接运行，无需安装 Python 或任何依赖**！

---

## 🎯 优势

### vs AppImage
| 方面 | PyInstaller | AppImage |
|------|-----------|---------|
| 文件大小 | ~100-150 MB | ~100-150 MB |
| 运行速度 | 更快（首次启动） | 一般 |
| 兼容性 | 高 | 非常高 |
| 启动时间 | 较快 | 稍慢 |
| 用户体验 | 直接点击运行 | 需要 chmod +x |

### vs Python 脚本
| 方面 | PyInstaller | Python 脚本 |
|------|-----------|----------|
| 依赖 | ❌ 无 | ✅ 需要 Python 3.7+ |
| pip | ❌ 无 | ✅ 需要 |
| 易用性 | ✅ 最简单 | ⚠️ 需要配置 |
| 安全性 | ✅ 二进制保护 | ⚠️ 源代码可见 |

---

## 📦 文件大小对比

```
PyInstaller 可执行文件:   ~120 MB (首次启动稍慢)
AppImage 文件:           ~150 MB (启动较快)
Python 脚本 + 虚拟环境:   ~200+ MB (需要 Python)
```

---

## 🚀 SteamDeck 上的使用

### 下载可执行文件

从 GitHub Releases 下载 `steamdeck-galgame` 文件

### 方式 1: 使用文件管理器（最简单）

```bash
# 1. 下载文件到 SteamDeck
# 2. 右键点击文件
# 3. 属性 → 权限 → 勾选"可执行"
# 4. 双击运行
```

### 方式 2: 使用命令行

```bash
# 1. 下载文件
cd ~/Downloads

# 2. 给予执行权限
chmod +x steamdeck-galgame

# 3. 直接运行
./steamdeck-galgame

# 或者放到任何位置
cp steamdeck-galgame ~/
~/steamdeck-galgame
```

### 方式 3: 创建快捷方式（推荐）

```bash
# 1. 放到应用目录
sudo mkdir -p /opt/steamdeck-galgame
sudo cp steamdeck-galgame /opt/steamdeck-galgame/

# 2. 创建快捷方式
cat > ~/.local/share/applications/steamdeck-galgame.desktop << 'EOF'
[Desktop Entry]
Version=1.0
Type=Application
Name=SteamDeck Galgame Config
Exec=/opt/steamdeck-galgame/steamdeck-galgame
Icon=io.github.steamdeck_galgame
Terminal=true
Categories=Utility;
EOF

# 3. 从应用菜单启动
```

---

## 🔨 本地打包（在 Linux/SteamDeck 上）

### 准备环境

```bash
# 1. 克隆项目
git clone <repo-url>
cd steamdeck-galgame

# 2. 安装依赖
pip install -r requirements.txt
pip install pyinstaller
```

### 执行打包

#### 方式 1: 自动打包脚本（推荐）

```bash
# 为当前系统编译
bash build_pyinstaller.sh

# 或使用交叉编译脚本
bash build_pyinstaller_crossplatform.sh native      # 本机编译
bash build_pyinstaller_crossplatform.sh x86_64      # 为 x86_64 编译
bash build_pyinstaller_crossplatform.sh aarch64     # 为 ARM64 编译
```

**交叉编译脚本优势：**
- ✅ 自动检测和验证依赖
- ✅ 为不同架构生成独立可执行文件
- ✅ 自动创建压缩分发包
- ✅ 详细的构建日志和帮助信息
- ✅ 支持 x86_64（SteamDeck）和 ARM64

#### 方式 2: 手动执行

```bash
pyinstaller \
    --name steamdeck-galgame \
    --onefile \
    --console \
    --add-data "data/icons:data/icons" \
    --hidden-import=src \
    --hidden-import=src.tui \
    --hidden-import=src.ui \
    --hidden-import=src.core \
    --hidden-import=src.core.installers \
    --hidden-import=src.utils \
    --hidden-import=requests \
    --strip \
    run.py
```

### 输出位置

```
dist/steamdeck-galgame                      # 可执行文件
dist/steamdeck-galgame-{arch}-YYYYMMDD.tar.gz  # 压缩分发包
```

---

## 🏗️ 交叉编译指南

### 什么是交叉编译？

交叉编译是指在一个系统架构上生成可在另一个架构上运行的可执行文件。例如，在 x86_64 Linux PC 上为 ARM64 生成可执行文件。

### 支持的架构

| 架构 | 说明 | 典型用途 |
|------|------|--------|
| `native` | 本机架构（自动检测） | 日常使用 |
| `x86_64` | 64 位 Intel/AMD | SteamDeck（大多数用户） |
| `aarch64` | ARM 64 位 | 未来扩展 |

### 使用交叉编译脚本

```bash
# 显示帮助信息
bash build_pyinstaller_crossplatform.sh --help

# 本机编译（推荐）
bash build_pyinstaller_crossplatform.sh

# 为特定架构编译
bash build_pyinstaller_crossplatform.sh x86_64      # 为 SteamDeck 编译
bash build_pyinstaller_crossplatform.sh aarch64     # 为 ARM64 编译
```

### 脚本功能

交叉编译脚本 `build_pyinstaller_crossplatform.sh` 提供以下功能：

✅ **自动依赖检查**
- 检查 Python 3 和 pip 是否安装
- 检查系统架构
- 提示缺失的依赖

✅ **架构检测**
- 自动识别当前系统架构
- 提示是否需要交叉编译
- 警告可能的兼容性问题

✅ **独立虚拟环境**
- 为每个架构创建独立的虚拟环境（venv_x86_64, venv_aarch64）
- 避免依赖冲突

✅ **自动构建**
- 自动安装 PyInstaller 和依赖
- 执行打包
- 验证输出

✅ **分发包**
- 生成可执行文件
- 自动创建压缩包（.tar.gz）
- 便于上传和分享

✅ **详细日志**
- 彩色输出，易于阅读
- 显示文件大小和架构信息
- 使用说明和下一步提示

### 输出示例

```
════════════════════════════════════════════════════════════════
  SteamDeck 中文环境配置工具 - PyInstaller 打包
════════════════════════════════════════════════════════════════

📋 检查系统依赖...
✅ python3 已安装
✅ pip3 已安装

ℹ️  Python 版本: 3.10.12
ℹ️  系统架构: x86_64

🔧 创建虚拟环境...
✅ 虚拟环境已创建

📦 安装 Python 依赖...
✅ 所有依赖已安装

🧹 清理旧构建文件...
🔨 开始打包...

✅ 打包完成！

════════════════════════════════════════════════════════════════
  输出文件位置
════════════════════════════════════════════════════════════════

✅ 可执行文件: dist/steamdeck-galgame (-120 MB)
✅ 压缩包: dist/steamdeck-galgame-x86_64-20260131.tar.gz

📋 使用方法：

1️⃣ 复制文件到 SteamDeck:
   scp dist/steamdeck-galgame deck@steamdeck:~/

2️⃣ 在 SteamDeck 上运行:
   chmod +x ~/steamdeck-galgame
   ~/steamdeck-galgame

✨ 无需 Python 和 pip！
```

### 在 CI/CD 中使用

交叉编译脚本也可在 GitHub Actions 中使用：

```yaml
- name: Build PyInstaller executable
  run: |
    bash build_pyinstaller_crossplatform.sh x86_64
    
- name: Upload to Release
  uses: softprops/action-gh-release@v1
  with:
    files: dist/steamdeck-galgame
```

---

## 🤖 GitHub Actions 自动打包

### 工作流文件

`.github/workflows/build-pyinstaller.yml`

### 自动触发条件

- ✅ 推送标签时（如 `v1.1.0`）
- ✅ 推送到 master 分支时
- ✅ 手动触发时

### 构建过程

```
推送标签或代码
    ↓
GitHub Actions 触发
    ↓
Ubuntu 环境
    ↓
Python + PyInstaller
    ↓
编译可执行文件
    ↓
自动创建 Release
    ↓
上传可执行文件
```

### 使用方式

```bash
# 1. 开发代码（在 Windows 上）
python run.py
python test_all.py

# 2. 推送代码
git add .
git commit -m "feat: xxx"
git push origin master

# 3. 创建版本
git tag -a v1.1.0 -m "Release"
git push origin v1.1.0

# 4. 等待自动构建（5-10 分钟）

# 5. 下载可执行文件
# GitHub Releases → 下载 steamdeck-galgame
```

---

## 📊 对比三种分发方式

### 1. PyInstaller 可执行文件 ⭐ 推荐

**优点：**
- ✅ 最简单（直接运行）
- ✅ 无需安装 Python
- ✅ 无需安装依赖
- ✅ 单个文件
- ✅ 安全（二进制编译）

**缺点：**
- ❌ 文件较大（~120 MB）
- ❌ 首次启动较慢（需要解压）

**适合：** 普通用户，一键启动

### 2. AppImage

**优点：**
- ✅ 文件可移植
- ✅ 启动较快
- ✅ 跨发行版兼容

**缺点：**
- ⚠️ 需要 chmod +x
- ⚠️ 文件较大

**适合：** 高级用户，跨发行版使用

### 3. Python 脚本

**优点：**
- ✅ 文件小（仅 ~10 MB）
- ✅ 易于修改

**缺点：**
- ❌ 需要安装 Python 3.7+
- ❌ 需要安装依赖（pip）
- ❌ 用户体验差

**适合：** 开发者，需要修改代码

---

## 🔍 故障排除

### 问题 1: 可执行文件无法运行

```bash
# 检查文件属性
file steamdeck-galgame
# 应该显示: ELF 64-bit executable

# 检查权限
ls -l steamdeck-galgame
# 应该有 x 标记

# 添加执行权限
chmod +x steamdeck-galgame
```

### 问题 2: 打包失败

```bash
# 检查 Python 版本
python3 --version
# 应该是 3.7+

# 检查依赖
pip list | grep pyinstaller

# 重新安装
pip install --upgrade pyinstaller
```

### 问题 3: 启动时缺少模块

```bash
# 检查 hidden-import 参数
# 在 pyinstaller 命令中添加缺失的模块
--hidden-import=missing_module
```

### 问题 4: 文件太大

```bash
# 使用 --onefile 参数（单文件）
# 使用 --strip 参数（移除调试符号）
# 使用 UPX 压缩（可选）
--upx-dir=/path/to/upx
```

---

## 📈 性能对比

### 启动时间

```
PyInstaller:   2-5 秒（首次较慢，后续缓存）
AppImage:      1-3 秒
Python 脚本:   1-2 秒（如已安装 Python）
```

### 内存占用

```
PyInstaller:   ~100-150 MB
AppImage:      ~100-150 MB
Python 脚本:   ~50-80 MB
```

### 文件大小

```
PyInstaller:   ~120 MB
AppImage:      ~150 MB
Python 脚本:   ~10 MB（+ 虚拟环境 ~200 MB）
```

---

## 🎓 推荐方案

### 对于普通用户（推荐）

**使用 PyInstaller 可执行文件**

```
优势：
✅ 完全无需依赖
✅ 最简单（直接点击运行）
✅ 最安全（二进制编译）
```

### 对于高级用户

**使用 AppImage**

```
优势：
✅ 跨发行版兼容
✅ 启动速度快
✅ 可验证签名
```

### 对于开发者

**使用 Python 脚本**

```
优势：
✅ 易于修改
✅ 文件最小
✅ 灵活扩展
```

---

## 🚀 下一步

### 立即使用（推荐）

```bash
# 1. 推送代码
git push origin master

# 2. 创建版本
git tag -a v1.1.0 -m "Release"
git push origin v1.1.0

# 3. 等待 GitHub Actions 自动构建

# 4. 下载可执行文件
# GitHub → Releases → steamdeck-galgame

# 5. 在 SteamDeck 上运行
chmod +x steamdeck-galgame
./steamdeck-galgame
```

---

## 📝 总结

| 方面 | PyInstaller | AppImage | Python |
|------|-----------|---------|--------|
| 易用性 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| 依赖 | ❌ 无 | ❌ 无 | ✅ 需要 |
| 启动速度 | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 文件大小 | 120 MB | 150 MB | 10 MB |
| 安全性 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **推荐** | ⭐⭐⭐ | ⭐⭐ | ⭐ |

---

**版本**: v1.0  
**日期**: 2026-01-31  
**状态**: ✅ 生产就绪
