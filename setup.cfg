[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 不运行集成测试
addopts = -v --tb=short

# 覆盖率相关
# addopts = --cov=src --cov-report=html --cov-report=term-missing
